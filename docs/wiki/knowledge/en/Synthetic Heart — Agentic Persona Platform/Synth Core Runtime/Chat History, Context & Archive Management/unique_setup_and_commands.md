Database tables are created lazily on first use via `init_chat_history_table()` and `init_chat_archives_table()`, which run `CREATE TABLE IF NOT EXISTS` plus a migration that adds the `metadata` JSON column if missing. The `chat_context_manager.initialize_context_manager()` must be called during core startup before any interface starts to ensure the cache table exists and persisted history is loaded.