"""
Development LLM Engines Package
================================

This directory contains experimental/development LLM engines that are only
loaded when the dev_components feature is enabled in the WebUI.

Safety: The dev_components flag resets to False on every restart, ensuring
that experimental code never persists in production environments.
"""
