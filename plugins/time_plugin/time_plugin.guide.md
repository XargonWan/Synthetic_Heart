# Time

Keeps Synth aware of **when and where she is**. It injects the current date,
both local and UTC time, and a configured location into every prompt, so time-
and date-related reasoning stays accurate.

## Actions

| Action | Purpose |
|--------|---------|
| `static_inject` | Inject current date, local/UTC time, and location into the prompt. |
