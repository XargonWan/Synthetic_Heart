"""Built-in Rift Vessel world connectors.

Connector modules in this package self-register with the Vessel registry at
import time (see :mod:`core.vessel_registry`). The Vessel plugin imports them on
startup; a missing or broken connector is non-fatal.
"""
