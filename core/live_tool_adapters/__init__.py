"""Format adapters that convert model-agnostic ``ToolManifest`` objects into
the wire-format declarations expected by each Live engine family.

Available adapters:
- ``core.live_tool_adapters.gemini``   — ``GeminiToolAdapter``
- ``core.live_tool_adapters.openai``   — ``OpenAIRealtimeToolAdapter`` (stub)
"""
