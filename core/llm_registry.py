"""Legacy LLM registry removed.

This module is intentionally empty to prevent legacy usage. Use
`core.cortex_registry` instead.
"""

raise RuntimeError(
    "Legacy LLM registry has been removed. Use core.cortex_registry instead."
)
