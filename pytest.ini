[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --disable-warnings
    --tb=short
markers =
    asyncio: marks tests as async (using pytest-asyncio)
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests

# Let pytest-asyncio handle unmarked async functions automatically
# (some tests use plain `async def` without @pytest.mark.asyncio)
asyncio_mode = auto